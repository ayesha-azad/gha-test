name: Docker Login
description: Write your own action to login to docker

inputs:
  username:
    description: Add username
  password:
    description: Add password 

runs:
  using: composite
  steps:
    - name: Login Docker
      run: echo "${{inputs.passwor}}" | docker login --username ${{inputs.username}} --password-stdin
      shell: bash

    - name: Check Failure
      if: failure()
      run: echo "Docker cannot login."
      shell: bash
